---
interface Props {
	title: string;
	tags?: string[];
	id?: string;
}

const { title, tags, id } = Astro.props;

import Tags from "./Tags.astro";
---

<section id={id}>
	<h2>{title}</h2>
	{tags && <Tags tags={tags} />}
	<slot />
</section>
